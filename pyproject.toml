[tool.poetry]
name = "mcp-browser-use"
version = "0.1.0"
description = ""
authors = ["Jules Whiteway <juleswhiteway@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
